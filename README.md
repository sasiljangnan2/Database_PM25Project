# 날짜와 가스데이터를 이용한 초미세먼지(PM2.5) 예측

**Ridge 회귀 · Random Forest 비교 분석**

> 대한민국 공공데이터(대기오염) 기반 — 시계열 특성 반영, 피처 엔지니어링, 모델 비교

---

## 1. 표지
- 프로젝트 제목:
- 교과목명:
- 담당 교수명:
- 팀원(학번 / 이름):
- 제출일:
- 팀명:

## 2. 요약 (Abstract)
프로젝트 전체 내용을 간략히 요약한다. 반 페이지 ~ 1페이지 이내 권장.

다음 항목을 포함할 것:
- 해결하고자 하는 문제: 날짜와 가스 데이터로 PM2.5(초미세먼지) 예측
- 사용한 데이터: 대한민국 공공데이터 포털(대기오염 관측 데이터)
- 적용한 모델: Ridge 회귀, Random Forest 회귀
- 주요 결과: (모델 성능 요약 표/핵심 수치 삽입)
- 최종 결론

## 3. 서론
연구 배경 및 필요성, 프로젝트 목표 등을 서술한다.

### 배경
초미세먼지(PM2.5)는 국민 건강 및 사회적 비용과 직결되는 중요한 환경 지표이다. 단기적 농도 예측은 취약계층 보호, 공공 경보, 환경 정책 수립에 기여할 수 있다. 가스 성분(NO2, O3, CO, SO2 등)은 PM2.5 형성에 영향을 주므로 이들만으로도 예측 가능한 모델 구축을 탐색한다.

### 프로젝트 목표
- 날짜(Time)와 가스 측정값을 입력으로 PM2.5(연속값)를 예측하는 모델 개발
- `Ridge`와 `Random Forest` 모델의 성능 비교 및 해석
- 시계열 특성(계절성·시차) 반영한 피처 엔지니어링과 시간 기반 검증 적용

## 4. 본론
프로젝트 수행 과정 및 적용 알고리즘을 상세히 기술한다. 표·다이어그램·그래프를 적극 활용.

### 4.1 관련 이론 및 기술
- **Ridge 회귀**: L2 정규화를 통한 안정적 선형 회귀(다중공선성 완화)
- **Random Forest 회귀**: 다수의 결정트리를 앙상블하여 비선형성 및 상호작용 포착, 변수 중요도 제공

### 4.2 데이터 설명 및 전처리
- **데이터 출처**: 대한민국 공공데이터 포털(https://www.data.go.kr/data/15122830/fileData.do)
- **주요 필드**: 측정 일시(또는 날짜), SO2, CO, NO2, O3, PM10, PM2.5 등
- **전처리 단계 요약**:
  - 결측치 처리(보간 또는 제거)
  - 이상치 탐지 및 처리(IQR 또는 표준편차 기반)
  - 시계열 정렬 및 필요 시 재샘플링
  - 스케일링(선형 모델용 표준화)
  - 피처 엔지니어링: lag(t-1, t-24 등), 이동평균(3h, 24h), 시간/요일/계절 더미
  - 학습/검증/테스트 분할: 시간 기반 분할로 데이터 누수 방지

### 4.3 모델 설계 및 실험 방법
- 파이프라인: 전처리 → 피처 생성 → 스케일링 → 모델 학습
- 하이퍼파라미터 탐색: `Ridge(alpha=[...])`, `RandomForest(n_estimators, max_depth, min_samples_leaf)`을 `TimeSeriesSplit` 또는 고정 검증셋으로 탐색
- 성능 지표: MAE, RMSE, R2(추가로 MAPE 고려)
- 검증전략: 시간 기반 교차검증(`TimeSeriesSplit`) 권장

### 4.4 실험 결과 및 분석
- 모델별 성능 표(예: MAE / RMSE / R2)
- 예측 vs 실제 시계열 플롯, 잔차 히스토그램
- Random Forest의 변수 중요도 분석
- 시간대·계절별 성능 비교(예: 겨울/봄 등)

> ※ 실험 수치 및 그래프는 실제 모델 실행 결과를 삽입하세요.

## 5. 결론
프로젝트 결과를 종합 정리한다.

포함 항목 예시:
- 수행한 작업 요약
- 최종 성능 및 가장 우수한 모델
- 모델 해석(주요 영향 변수)
- 한계점 및 향후 개선 방향(기상 데이터 결합, 시공간 모델, 시계열 전용 모델 등)

## 6. 참고문헌
- 사용한 데이터, 라이브러리 문서, 참고 논문/강의자료 등

