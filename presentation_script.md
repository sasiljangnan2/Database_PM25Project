# 초미세먼지(PM2.5) 예측 프로젝트 발표 대본

**발표자 시나리오 (PPT 슬라이드 흐름 기준)**
*※ 아직 실험 결과와 결론 수치가 도출되지 않은 점을 반영하여 전반적인 프로젝트 기획과 과정, 향후 계획에 초점을 맞춘 대본입니다.*

---

## Slide 1: 표지 (인사 및 주제 소개)
"안녕하세요. '날짜와 가스 데이터를 이용한 초미세먼지 예측' 프로젝트 발표를 맡은 9조 입니다. 저희는 대한민국 공공데이터포털의 대기오염 관측 데이터를 바탕으로 다양한 머신러닝 회귀 모델을 통해 초미세먼지 수치를 예측하고 비교해보았습니다."

## Slide 2: 요약 (Abstract)
"먼저 전체 프로젝트 요약입니다. 본 프로젝트의 목적은 날짜 요소와 이산화질소, 오존 등 주변 가스 수치만을 이용해 PM2.5, 즉 초미세먼지 농도를 연속값으로 예측하는 것입니다. 이를 위해 선형 모델의 단점을 보완한 Ridge, Lasso 회귀 모델과 대표적인 앙상블 트리 모델인 Random Forest 회귀 모델을 적용하여 각각의 특성과 성능을 비교 분석하는 방식으로 프로젝트를 구성했습니다."

## Slide 3: 서론 (배경 및 목표)
"다들 아시다시피 초미세먼지는 호흡기 질환 등 건강과 직결되는 매우 중요한 지표입니다. 보통 초미세먼지는 복잡한 기후 조건의 영향을 받지만, 이번 연구에서는 가스 성분(NO2, O3, CO, SO2 등)만으로도 유의미한 예측이 가능한지 가능성을 탐색해보고자 했습니다. 
또한 시계열 특성이 있는 데이터를 다룸으로써 시간 변화(계절, 시간대)가 타겟에 미치는 영향을 파악하고, 모델 간의 성능 차이와 해석 방법을 도출하는 것을 주된 목표로 삼았습니다."

## Slide 4: 관련 이론 (사용 알고리즘 및 주요 파라미터 개념)
"저희가 실험에 사용할 세 가지 예측 모델을 크게 두 가지 계열로 나누어, 핵심 원리와 제어 가능한 주요 파라미터에 대해 설명드리겠습니다. 

첫 번째 계열은 기존 단순 선형 회귀의 단점을 보완한 **선형 회귀 기반 정규화 모델**로, 릿지(Ridge)와 라쏘(Lasso)가 여기에 속합니다. 
기본적인 선형 회귀는 변수가 많아지거나 상관관계가 높을 때 가중치가 통제할 수 없이 커지면서 새로운 데이터의 예측을 완전히 망쳐버리는 '과적합(Overfitting)' 문제가 자주 발생합니다. 이 두 모델은 모델의 오차를 줄이려 노력하면서도 동시에 이 '가중치가 커지는 것'을 억제하도록 비용 함수에 페널티를 더해 제약하는 기법들입니다. 서로 목적은 같지만, 사용되는 수학적 제약 방식에 따라 모델의 결과 성향이 완전히 달라집니다.

먼저 **릿지(Ridge) 회귀**입니다. 
릿지는 '회귀 계수들의 제곱합'인 'L2 패널티'를 사용합니다. 이 방식은 불필요한 변수라고 할지라도 가중치를 완전히 0으로 없애버리지는 않고 그 크기만 0에 가깝게 줄 작동합니다. 결과적으로 입력된 모든 가스 변수를 유지하면서 변수들 간의 다중공선성을 완화시켜, 튀는 값 없이 예측을 매우 안정적으로 만들어 주는 것이 릿지의 가장 큰 특징입니다.

다음으로 **라쏘(Lasso) 회귀**입니다. 
라쏘는 '회귀 계수들의 절댓값 합'이라는 'L1 패널티'를 부여합니다. 이 방식은 마름모꼴의 기하학적 특성 때문에, 초미세먼지 예측에 기여도가 덜한 일부분의 수많은 가스 성분 가중치를 수학적으로 '완전히 0'으로 만들어버립니다. 따라서 진짜로 중요한 요인들만 모델에 남게 되는 강력한 **자동 변수 선택(Feature Selection)** 효과를 가져오며, 모델 결과를 직관적으로 해석할 때 아주 좋은 장점을 가집니다.

물론, 이 두 선형 모델에서 공통적으로 저희가 가장 중요하게 조절해야 할 하이퍼파라미터는 저 패널티들의 강도를 통제하는 **알파(alpha)** 값입니다.

두 번째 계열은 강력한 **앙상블 트리 모델인 Random Forest 회귀**입니다.
대기오염 데이터는 가스 농도나 시간대별 패턴 등 변수들 간의 관계가 다소 복잡하기 때문에, 비선형적 패턴을 잡아내기 위해 활용했습니다. 수많은 결정 트리를 부트스트랩 방식으로 무작위 샘플링하여 학습시키고 그 결과들을 종합해 평균을 내는 배깅(Bagging) 기법을 사용하므로, 개별 트리들이 가진 과대적합 요소를 상쇄시켜 예측이 아주 견고해지고 나아가 각 변수별 중요도(Feature Importance) 분석도 가능해집니다.
이 모델의 성능 분석과 과적합 제어를 위해서는 크게 3가지 하이퍼파라미터가 중요하게 다뤄집니다. 숲을 구성할 '전체 트리의 개수(n_estimators)', 트리가 무작위적으로 무한정 뻗어 나가지 못하게 제한하는 '트리의 최대 깊이(max_depth)', 그리고 각 노드에서 분할 기준으로 사용할 '무작위 특징 후보의 개수(max_features)'입니다."

## Slide 5: 데이터 설명 및 전처리
"다음은 데이터 파트입니다. 저희는 2024년 성북구 대기오염 관측 데이터를 사용했습니다.
유의미한 결과를 도출하기 위해 몇 가지 필수적인 전처리를 진행했습니다. 우선 모델이 시간의 흐름을 잘 학습할 수 있도록, 문자열로 된 '측정일시' 변수에서 '월(Month)'과 '시간대(Hour)'를 정수 형태로 추출하여 새로운 파생 변수를 만들었습니다.
이후 학습에 방해가 되거나 불필요한 측정소 식별 정보, 타겟과 중복될 수 있는 PM10(미세먼지) 등을 제거하고 결측치가 있는 데이터를 깔끔하게 삭제하여 분석용 데이터셋을 완성했습니다."

## Slide 6: 모델 설계 및 실험 방법 (하이퍼파라미터 설정)
"설계된 모델의 실험 방법과 중요한 하이퍼파라미터 설정에 대해 설명드리겠습니다. 평가의 객관성을 위해 전체 데이터는 8대 2 비율로 나누어 학습과 테스트에 사용합니다. 

우선 선형 회귀 계열 모델인 **Ridge와 Lasso**는 변수의 스케일에 민감하므로 데이터를 정규화하는 스케일링 작업을 반드시 선행합니다. 이 두 모델에서 가장 중요한 하이퍼파라미터는 **규제 강도를 의미하는 '알파(alpha)'**입니다. 알파 값을 조절해 가며 모델이 학습 데이터에 과적합되지 않으면서도 안정적인 예측을 할 수 있는 최적의 페널티 수준을 찾도록 설계했습니다.

반면, 앙상블 모델인 **Random Forest**의 경우 세 가지 주요 하이퍼파라미터를 설정했습니다. 성능과 안정성을 확보하기 위해 **전체 트리의 개수(n_estimators)**는 100개로 지정하였고, 트리가 너무 편향되게 학습되어 과적합되는 것을 막기 위해 **트리의 최대 깊이(max_depth)**는 10으로 제한했습니다. 또한, 각 노드에서 분기할 때 고려할 **무작위 피처의 수(max_features)**도 함께 조절하여 비선형적인 패턴을 가장 효과적으로 학습할 수 있도록 구성했습니다.

최종 성능 평가 지표로는 대표적인 회귀 지표인 평균 절대 오차(MAE), 루트 평균 제곱 오차(RMSE), 그리고 결정 계수(R2-Score)를 활용할 계획입니다."

## Slide 7: 실험 결과 예측 및 결론 (향후 계획)
"현재 각 모델의 구체적인 결과 지표는 도출 및 고도화하는 단계에 있습니다. 실험이 마무리되면 저희는 앙상블 모델의 변수 중요도(Feature Importance)와 선형 모델들의 가중치 크기를 통해, 어떤 가스 혹은 특정 월이나 시간대가 초미세먼지 농도에 가장 치명적인 영향을 끼쳤는지 비교 분석할 예정입니다.
이를 통해 단순히 높은 성능의 모델을 찾는 것을 넘어, 대기오염 예측에 있어서의 한계점과 기상 데이터를 추가로 결합했을 때 기대되는 향후 개선 방향까지 구체화할 계획입니다.

이상으로 발표를 마치겠습니다. 경청해 주셔서 감사합니다."