# 초미세먼지(PM2.5) 예측 프로젝트 발표 대본

**발표자 시나리오 (PPT 슬라이드 흐름 기준)**
*※ 아직 실험 결과와 결론 수치가 도출되지 않은 점을 반영하여 전반적인 프로젝트 기획과 과정, 향후 계획에 초점을 맞춘 대본입니다.*

---
0
## Slide 1: 표지 (인사 및 주제 소개)
"안녕하세요. '날짜와 가스 데이터를 이용한 서울 성북구의 PM2.5초미세먼지 예측' 프로젝트 발표를 할 9조 입니다. 저희는 대한민국 공공데이터포털의 대기오염 관측 데이터를 바탕으로 다양한 머신러닝 회귀 모델을 통해 초미세먼지 수치를 예측하고 비교해보았습니다."

## Slide 3: 서론 (배경 및 목표)
"연구 배경과 목표를 말씀드리겠습니다. 초미세먼지는 우리의 일상생활과 호흡기 건강을 결정짓는 핵심적인 환경 지표입니다. 만약 이 초미세먼지 농도를 단기적으로 세밀하게 예측해 낼 수만 있다면, 노약자 등 취약계층을 보호하고 신속하게 공공 경보를 발령하는 등 실생활의 여러 방면에서 큰 도움이 될 것입니다.

보통 초미세먼지는 비나 바람, 기온 같은 복잡한 기상 조건의 영향을 무척 크게 받습니다. 하지만 저희는 외부 기상 데이터 없이, 오직 '시간 데이터'와 매일 측정소에서 관측되는 '주변 가스 성분(NO2, O3, CO, SO2 등)' 농도만으로도 의미 있는 수준의 예측 모델을 만들어낼 수 있을지 그 가능성을 집중적으로 파헤쳐보고자 했습니다.

저희의 주된 목표는 세 가지입니다. 
첫째, 시간과 가스 데이터만을 활용한 '초미세먼지 단기 예측 모델 구현'입니다. 
둘째, 모델 간의 객관적인 '성능 비교 및 장단점 분석'입니다. 특히 대기 중의 가스 데이터들은 배출 원인이 서로 비슷해 농도가 다 함께 오르내리는 경향이 강합니다. 이렇게 예측에 쓰이는 변수들끼리 너무 끈끈하게 얽혀 있어 오히려 예측 과정을 방해하는 현상을 **'다중공선성'**이라고 합니다. 저희는 이 골치 아픈 다중공선성을 스스로 완화해 내는 릿지, 라쏘 등의 선형 모델들과, 아예 복잡한 비선형적 관계로 해석하는 랜덤 포레스트의 특징을 대조해 볼 것입니다.
마지막으로, 어느 계절이나 시간대에 초미세먼지가 극심해지는지 모델을 통해 그 '핵심 시계열 특성과 가중 요인'을 명확히 파악하는 것입니다."

## Slide 4: 관련 이론 (사용 알고리즘 및 주요 파라미터 개념)
"저희가 주로 실험에 사용한 세 가지 예측 모델의 핵심 원리와 극명한 차이점, 그리고 주요 파라미터에 대해 설명드리겠습니다. 화면의 비용 함수 수식을 함께 집중해 주시길 바랍니다.

저희가 설계한 첫 번째 모델은 **릿지(Ridge) 회귀**입니다. 
여기서 
RSS는 잔차 제곱합(실제값과 예측값의 차이)을 의미하며, wj
는 각 독립 변수의 회귀 계수(가중치)를 뜻합니다. 수식 뒷부분의 α∑j=1pwj2 가 바로 L2 페널티 항입니다.
알파와 규제 항의 크기를 곱해 제약 강도를 조절해줍니다.
일반적인 선형 회귀는 변수가 많거나 상관관계가 높을 때 가중치를 제어하지 못해 새로운 예측을 망치는 '과적합' 현상이 흔히 발생합니다. 릿지는 이를 막기 위해 비용 함수($Cost$) 끝부분에 가중치($w_j$)들의 제곱합항을 더해 제약을 가하는데요, 이렇게 제곱 을 주게 되어 가중치가 완전히 0이 되진 않습니다. 즉, **모든 가스 변수를 살려둔 채** 전체적인 가중치를 줄여, 서로 얽혀있는 가스 농도들 사이의 다중공선성을 부드럽게 완화하고 모델을 안정화하는 데 탁월합니다.

두 번째 모델은 **라쏘(Lasso) 회귀**입니다. 
가중치를 억제하려는 목적은 릿지와 같으나, 식을 보시면 가중치들의 '절댓값의 합'($|w_j|$) 항을 사용합니다. 이것이 l1 페널티 항입니다.
불필요한 가스 변수의 계수를 **'0'으로 수렴**시켜버립니다.
그 결과 모델 예측에 직접적으로 영향을 주는 소수의 핵심 가스 요인만 살아남게 되어 이른바 모델의 **희소성(Sparsity)**이 높아지고, 자연스럽게 **자동 변수 선택(Feature Selection)** 효과를 불러와 사람이 직관적으로 해석하기에 매우 좋습니다.

여기서 릿소회귀와 랏소 회귀의 차이를 집고 넘어가보자면 릿지가 모든 변수를 안고 가는 반면, 오차($RSS$)를 최소화하는 과정에서 절댓값 제0약을 받는 라쏘는 초미세먼지 예측에 기여도가 적은 가스 변수의 가중치를 수학적으로 정확히 **'0'으로 탈락**시켜버립니다.  
다만 라쏘는 이번 대기오염 데이터처럼 서로 의미가 겹치거나 상관관계가 높은 가스 변수들이 다수 존재할 때, 그중 하나만 임의로 살리고 나머지는 날려버려 예측을 불안정하게 할 위험성도 내포하고 있다는 뚜렷한 한계가 있습니다.


마지막은 단일 트리의 한계를 뛰어넘은 앙상블 트리 모델, **Random Forest 회귀**입니다.
수많은 결정 트리를 무작위 샘플링으로 뽑아 모으고 그 결과들을 합쳐서 평균을 내는 배깅(Bagging) 기법을 씁니다. 하나의 트리가 너무 튀게 학습해버리는 요소를 다수의 트리가 상쇄시켜, 정확한 예측을 만들어 줍니다.
이 모델의 성능을 끌어올리고 과대적합을 막기 위해 크게 세 가지 하이퍼파라미터가 다뤄집니다. 숲에 심을 '전체 트리의 개수(n_estimators)', 트리가 무작정 뻗어 나가지 못하도록 자르는 '트리의 최대 깊이(max_depth)', 마지막으로 노드를 나눌 때 사용할 '무작위 특징 후보의 개수(max_features)'입니다."

## Slide 5: 데이터 설명 및 전처리
"다음은 데이터 파트입니다. 저희는 2024년 성북구 대기오염 관측 데이터를 사용했습니다.
유의미한 결과를 도출하기 위해 몇 가지 필수적인 전처리를 진행했습니다. 우선 성북구의 데이터와 필요한 데이터를 분리하고,
모델이 시간의 흐름을 잘 학습할 수 있도록, 문자열로 된 '측정일시' 변수에서 '월(Month)'과 '시간대(Hour)'를 정수 형태로 추출하여 새로운 파생 변수를 만들었습니다.
이후 학습에 방해가 되거나 불필요한 측정소 식별 정보, 타겟과 중복될 수 있는 PM10(미세먼지) 등을 제거하고 결측치가 있는 데이터를 깔끔하게 삭제하여 분석용 데이터셋을 완성했습니다."

## Slide 6: 데이터 분석 환경
"본격적인 모델 학습에 앞서 전체적인 실험 세팅 환경에 대해 먼저 말씀드리겠습니다. 전체 데이터는 과적합 없이 객관적으로 성능을 평가하기 위해 모델 학습용(Train) 8, 검증용(Test) 2의 비율로 깔끔하게 분할했습니다.
실험 결과의 일관성 및 재현성을 위해 난수 시드(`random_state=42`)를 고정하여 무작위 샘플링을 진행했습니다.
동시에, 선형 모델들은 데이터 단위에 무척 예민하게 반응하므로 화면에 보이시는 정규화 공식처럼 모든 가스 변수의 평균을 0, 분산을 1로 맞추어주는 `StandardScaler` 스케일링 작업을 선행하여 학습의 안정성을 잡았습니다.
이렇게 세팅된 모델들을 최종적으로 어떻게 평가할 것인가 하는 '성능 평가 지표'로는 여러분이 잘 아시는 실제 오차 크기인 MAE와 큰 오차에 패널티를 주는 RMSE, 그리고 우리 모델이 데이터를 몇 %나 설명하는지 단번에 알려주는 결정 계수(R2-Score)를 채택해 체계적인 실험 프레임워크를 구성했습니다."

## Slide 7: 선형 회귀 모델(Ridge, Lasso)의 최적화 및 실험 결과
"이렇게 구축된 환경 위에서 각 모델이 스스로 최고의 성능을 낼 수 있도록 **하이퍼파라미터 튜닝**을 진행했습니다. 

먼저 **릿지(Ridge) 회귀**입니다. 릿지는 모델에 가하는 제약의 강도, 즉 **알파(alpha)** 값을 통제하는 것이 핵심입니다. 범위 내 수많은 테스트를 거쳐 다중공선성을 가장 안정적으로 잡아내는 최적의 알파 값을 '1'로 도출했습니다.
다음으로 **라쏘(Lasso) 회귀**입니다. 라쏘는 변수를 완전히 0으로 만들어 제거해 버리는 특징이 있기 때문에, 알파 값을 미세하게 깎아내려 '0.001'이라는 최적값을 찾아냈고 유의미한 가스 변수들을 보존하며 오차를 최소화했습니다.

하지만 화면 아래에 첨부된 선형 모델의 **'예측 결과 및 회귀계수 그래프'**를 보시면 선형 모델의 뚜렷한 한계가 적나라하게 드러납니다. 왼쪽 산점도를 보시면 실제 초미세먼지 수치가 높아질수록 모델의 예측점이 이를 따라가지 못하고 가로로 넓게 퍼져 있습니다. 결국 정규화를 거쳤음에도 이들 선형 모델 3종의 R2-Score는 0.517 선에서 답보 상태에 머물렀습니다. 
반대로 오른쪽의 회귀계수 결과 막대를 보면, 선형 모델들은 일산화탄소(CO)에 가장 큰 양(+)의 가중치를 주고, 계절을 타는 '월(Month)' 변수에는 음(-)의 가중치를 주어 예측을 시도하려 했음을 쉽게 파악하실 수 있습니다."

## Slide 8: 비선형 모델(Random Forest) 최적화 및 핵심 요인 분석
"다음은 선형 방정식의 한계를 극복하기 위해 도입한 비선형 앙상블 모델, **Random Forest**입니다. 예측 속도와 성능 밸런스를 위해 전체 트리의 개수(n_estimators)는 100개로 고정하고, 과적합을 막기 위해 트리의 최대 깊이(max_depth)를 10까지만 뻗게 쳐내어 안정적으로 통제했습니다. 더불어 노드 분할 시 사용하는 변수의 수(max_features)는 수정을 가하지 않고 기본값을 그대로 두었는데요, 이를 통해 모델이 임의로 변수를 쳐내지 않고 우리가 가진 모든 가스와 시간 데이터를 매번 온전히 고려하며 제일 정교한 패턴을 학습하도록 꽉 잡아주었습니다.

결과는화면 속 산점도에서 보시듯 빨간색 정답 선을 따라 군집이 훨씬 더 오밀조밀하게 잘 뭉쳐있어, 절대 오차(MAE) 수치가 5.1 수준으로 뚝 떨어졌고 R2 데이터 설명력 역시 0.658로 크게 도약하며 더 좋은 성적을 거두었습니다.

특히 예측 성능을 끌어올릴 수 있었던 비결은 좌측에 띄워진 **'Random Forest 변수 중요도(Feature Importance) 그래프'**에 있습니다. 수많은 분기를 거친 끝에 찾아낸 핵심 인자 순위인데, 복잡한 가스 구조 속에서도 **'일산화탄소(CO)'**가 이전과 동일하게 높은 중요도를 보이고 있습니디. 차이점은 시계열 변수인 월(Month)이 초미세먼지 메커니즘에서 이전 릿지 라쏘 모델보다  큰 비중을 차지했다는 점인데요,

이러한 결과는 우측의 **'월별 초미세먼지 평균 농도 막대그래프'**를 보시면 더 명확해집니다. 모델이 왜 계절성 요소인 '월'을 중요하게 취급했는지가 뚜렷하게 나옵니다. 1월부터 4월(겨울~봄) 기간에는 파란 막대가 높이 치솟아 있지만, 비가 잦고 대기 확산이 활발한 7~9월(여름~가을)에는 주황 막대로 수치가 대폭 떨어지는 'U자형 계절 패턴'이 육안으로 확인되실 겁니다. 대기오염은 일산화탄소의 급증과 뚜렷한 계절성 등이 복합적으로 작용하므로, 이런 조건을 다각도로 엮어내는 랜덤 포레스트가 훨씬 효과적이었습니다."

## Slide 9: 종합 결론 및 향후 과제
"이러한 실험들을 바탕으로 내린 저희 조의 최종 결론입니다. 화면 하단에 띄워진 **'모델별 종합 성능(R2) 비교 막대그래프'**를 주목해 주시기 바랍니다.

이를 보시면 가스 농도의 단순 덧셈 뺄셈으로 구성된 선형 모델 3종은 모두 파란색, 초록색, 빨간색 막대처럼 R2 점수가 0.517 선에서 명확한 한계에 부딪혔습니다. 반면 비선형적인 패턴을 추적해 낸 보라색의 랜덤 포레스트 막대는 0.658로 성능을 크게 도약시켰습니다. 결론적으로 대기오염처럼 다층적인 현상을 예측하기 위해서는 선형 방정식이 아닌, 가스 간 상호작용과 계절성을 입체적으로 파악하는 앙상블 기법을 적용해야만 의미 있는 성능 상승과 요인 분석이 가능했음을 최종적으로 확인했습니다.

다만 본 연구에는 아직 34%라는 미설명 오차가 남아있습니다. 이는 예측에 결정적인 영향을 미치는 강수량, 풍향, 기온 등 기상 요인이 배제되었기 때문입니다. 향후 기상청의 핵심 관측 데이터를 추가로 결합한다면 오차를 훨씬 줄일 수 있을 것입니다. 나아가 단순 회귀를 넘어 **LSTM, GRU 등 딥러닝 시계열 모델**로 예측 프레임워크를 확장한다면, 어제와 오늘의 공기 흐름이 내일에 어떻게 연속적으로 미치는지까지 오차 없이 파악하는 훌륭한 대기오염 예보 시스템이 완성될 것입니다.

가스와 시간 데이터만으로도 유의미한 초미세먼지 예측의 토대를 다졌다는 점을 다시 한번 강조해 드리며 발표를 마치겠습니다. 경청해 주셔서 감사합니다."